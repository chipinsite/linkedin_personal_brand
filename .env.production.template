# ══════════════════════════════════════════════════════════════════
# LinkedIn Personal Brand Autoposter — Production Environment
# ══════════════════════════════════════════════════════════════════
# Copy this file to .env and fill in the required values.
# Lines marked [REQUIRED] must be set before deployment.
# Lines marked [OPTIONAL] have sensible defaults.
#
# For Railway: set these in the Railway dashboard → Variables tab.
# For Docker Compose: save as .env in the project root.
# ══════════════════════════════════════════════════════════════════

# ── Database ──────────────────────────────────────────────────
# [REQUIRED for Docker Compose — Railway provides this automatically]
POSTGRES_USER=linkedbrand
POSTGRES_PASSWORD=CHANGE_ME_strong_password_here
POSTGRES_DB=linkedbrand

# Full connection string (Railway injects DATABASE_URL automatically)
# DATABASE_URL=postgresql+psycopg://linkedbrand:CHANGE_ME@postgres:5432/linkedbrand

# ── Redis ─────────────────────────────────────────────────────
# [Railway provides REDIS_URL automatically]
# REDIS_URL=redis://redis:6379/0

# ── Authentication ────────────────────────────────────────────
# [REQUIRED] Generate with: openssl rand -hex 32
JWT_SECRET_KEY=CHANGE_ME_run_openssl_rand_hex_32

# ── Claude AI (Content Generation) ───────────────────────────
# [REQUIRED] Get from: https://console.anthropic.com/settings/keys
LLM_API_KEY=sk-ant-CHANGE_ME

# Set to true to run without a real Claude API key (uses template content)
LLM_MOCK_MODE=false
LLM_MODEL=claude-sonnet-4-20250514

# ── Telegram Bot (Approval Workflow) ─────────────────────────
# [REQUIRED] Create bot via @BotFather on Telegram
TELEGRAM_BOT_TOKEN=CHANGE_ME

# [OPTIONAL] Restrict bot to a specific chat. Get your chat ID
# by messaging @userinfobot on Telegram.
TELEGRAM_CHAT_ID=

# ── LinkedIn API ──────────────────────────────────────────────
# [OPTIONAL] Manual publish mode works without API access
LINKEDIN_API_MODE=manual
LINKEDIN_API_TOKEN=

# ── Frontend ──────────────────────────────────────────────────
# [REQUIRED] Set to your backend's public URL
VITE_API_BASE_URL=https://your-backend.railway.app

# [REQUIRED] Set to your frontend's public URL for CORS
CORS_ALLOWED_ORIGINS=https://your-frontend.railway.app

# ── Ports (Docker Compose only) ──────────────────────────────
API_PORT=8000
FRONTEND_PORT=3000

# ── Scheduling ────────────────────────────────────────────────
# [OPTIONAL] All defaults are production-ready
TIMEZONE=Africa/Johannesburg
POSTING_WINDOW_START=08:00
POSTING_WINDOW_END=17:00
POSTING_ENABLED=true
COMMENT_REPLIES_ENABLED=true
MAX_AUTO_REPLIES=5

# ── Research Sources ──────────────────────────────────────────
RESEARCH_FEED_URLS=https://digiday.com/feed/,https://www.adexchanger.com/feed/,https://www.exchangewire.com/feed/
